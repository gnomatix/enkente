{
  "name": "enkente-chat-bridge",
  "displayName": "enkente Chat Bridge",
  "description": "Intercepts VS Code chat messages and forwards them to the enkente ingestion API",
  "version": "0.1.0",
  "publisher": "gnomatix",
  "license": "BSL-1.1",
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "enkente Chat Bridge",
      "properties": {
        "enkente.apiUrl": {
          "type": "string",
          "default": "http://localhost:8080",
          "description": "The URL of the enkente ingestion API"
        },
        "enkente.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable the chat bridge"
        },
        "enkente.identityProvider": {
          "type": "string",
          "default": "auto",
          "enum": ["auto", "github", "microsoft", "gpg", "env", "anonymous"],
          "enumDescriptions": [
            "Automatically detect the best available identity (GitHub → Microsoft → env → anonymous)",
            "Use the logged-in GitHub account name",
            "Use the logged-in Microsoft account name",
            "Use the GPG key fingerprint as a stable cryptographic user token",
            "Use the OS username from environment variables",
            "Generate a deterministic anonymized hash from the resolved identity"
          ],
          "description": "Which identity provider to use for attributing chat messages"
        }
      }
    },
    "commands": [
      {
        "command": "enkente.toggle",
        "title": "enkente: Toggle Chat Bridge"
      },
      {
        "command": "enkente.status",
        "title": "enkente: Show Bridge Status"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/vscode": "^1.93.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
